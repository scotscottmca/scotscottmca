---
import { SITE, NAV } from '../config';
---

<header class="site-header">
  <div class="container">
    <a class="brand" href="/"><span class="led"></span> {SITE.title}</a>
    <nav class="nav" aria-label="Primary">
      {NAV.map((item) => <a href={item.href}>{item.label}</a>)}
    </nav>
  </div>
</header>
